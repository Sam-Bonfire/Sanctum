package com.sanctum.core.feature.prayer.domain

enum class AsrJuristicMethod {
    STANDARD_SHAFII, // Shadow length = 1x object height + noon shadow
    HANAFI, // Shadow length = 2x object height + noon shadow
}
