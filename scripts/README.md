# Scripts

Live tasks (also in `mise.toml`): `db:generate`, `assets:sync`, `assets:fetch`,
`seo:validate`, `seo:generate-metadata`, `fonts:download`.

Retired one-shots (kept for history, do not wire into the runner): `fetch_quran`,
`fetch_bible`, `fetch_bibles`, `fetch_bibles_fast`, `fetch_buddhism`,
`fetch_hinduism_expanded`, `fetch_jainism`, `fetch_jewish`, `fetch_shinto`,
`fetch_sikhism`, `fix_christianity_chapter_ids`, `merge_scripture`,
`rebuild_christianity`, `rename_app` (already applied), `wati_websocket`
(unrelated debug script — rotate its hardcoded token if still valid).
